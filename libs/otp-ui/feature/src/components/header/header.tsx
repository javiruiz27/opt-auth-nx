import { Typography } from '@mui/material';
import React from 'react';

export const Header = () => {
  return (
    <div>
      <Typography variant="h1">OTP Authentication</Typography>
    </div>
  );
};
